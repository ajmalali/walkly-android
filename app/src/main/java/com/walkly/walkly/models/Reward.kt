package com.walkly.walkly.models

interface Reward {
    // Reward is either equipment or (consumable) item
    // This interface should hold the common functions that are required to
    //    be able to reward one after winning a battle

}
